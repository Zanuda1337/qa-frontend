import {axiosInstance} from "src/api";import {	IAddQuestionRequest,	IEditQuestionRequest,	TQuestionResponse} from "src/features/questions/Questions.types";export const questionsApi = {	fetchQuestions: () => axiosInstance.get('/questions'),	fetchOneQuestion: (id: number) => axiosInstance.get<TQuestionResponse>(`/questions/${id}`),	createQuestion: (data: Omit<IAddQuestionRequest, 'id'>) => axiosInstance.post<TQuestionResponse>('/questions', data),	updateQuestion: ({id, answer, title, categoryId}: IEditQuestionRequest) => axiosInstance.put<TQuestionResponse>(`/questions/${id}`, {answer, title, categoryId}),	deleteQuestion: (id: number) => axiosInstance.delete(`/questions/${id}`),}