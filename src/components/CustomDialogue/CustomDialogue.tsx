import React from 'react';import classes from './CustomDialogue.module.scss'import {	Dialog, DialogActions,	DialogContent,	DialogContentText,	DialogTitle} from "@mui/material";import CustomButton from "src/components/CustomButton/CustomButton";type TCustomDialogueProps = {	open: boolean;	content: string | JSX.Element;	title: string;	style?:  React.CSSProperties	fetching?: boolean;	confirmTitle?: string;	onClose?: () => void;	onConfirm?: () => void;}const CustomDialogue: React.FC<TCustomDialogueProps> = ({open, content,fetching,style, title, confirmTitle = 'Подтвердить', onClose, onConfirm}) => {	return (		<Dialog			open={open}			onClose={onClose}			style={style}			classes={{root: classes.root}}		>			<DialogTitle>				{title}			</DialogTitle>			<DialogContent>				<DialogContentText>					{content}				</DialogContentText>			</DialogContent>			<DialogActions>				<CustomButton onClick={onClose}>Отмена</CustomButton>				<CustomButton onClick={onConfirm} fetching={fetching}>					{confirmTitle}				</CustomButton>			</DialogActions>		</Dialog>	);};export default CustomDialogue;