import React, { useEffect, useRef, useState } from 'react';import classes from './ProgressBar.module.scss';type TProgressBarProps = {  progress: number;  color?: string;};const ProgressBar: React.FC<TProgressBarProps> = ({  progress,  color = 'red',}) => {  const barRef = useRef<HTMLDivElement>(null);  const [width, setWidth] = useState(0);  useEffect(() => {    if (!barRef.current) return;    setWidth(barRef.current.getBoundingClientRect().width);  }, [barRef, progress]);  const value = progress / 100;  const progressWidth = width * value  return (    <div className={classes.bar} ref={barRef}>      <div        className={classes.progress}        style={{ background: color, marginLeft: progressWidth - width }}      />    </div>  );};export default ProgressBar;