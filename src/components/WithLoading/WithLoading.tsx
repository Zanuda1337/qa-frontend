import React from 'react';import {Box, CircularProgress} from "@mui/material";type TWithLoadingProps = {	children: JSX.Element;	fetching?: boolean;	size?: number}const WithLoading: React.FC<TWithLoadingProps> = ({children, fetching, size = 24}) => {	return (		<Box sx={{ position: 'relative' }}>			{children}			{fetching && (				<CircularProgress					size={size}					sx={{						position: 'absolute',						left: 0,						top: 0,						bottom: 0,						right: 0,						margin: 'auto',					}}				/>			)}		</Box>	);};export default WithLoading;