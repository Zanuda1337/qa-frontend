import React from 'react';import classes from './Navigation.module.scss';import Frontend from 'src/assets/images/frontend.png';import { Button } from '@mui/material';import SvgSelector from 'src/components/SvgSelector/SvgSelector';import { clsx } from 'clsx';import { useLocation, useNavigate } from 'react-router-dom';type TNavigationProps = {};const links = [  { path: '/admin/questions', icon: 'quiz' },  { path: '/admin/categories', icon: 'category' },];const Navigation: React.FC<TNavigationProps> = ({}) => {  const navigate = useNavigate();  const location = useLocation();  return (    <nav className={classes.navigation}>      <div className={classes.wrapper}>        <div className={classes.logo}>          <img src={Frontend} alt="" />        </div>        <ul>          {links.map((link) => (            <li className={classes.link} key={link.path}>              <Button                fullWidth                className={clsx({                  [classes.active]: link.path === location.pathname,                })}                onClick={() => navigate(link.path)}              >                <SvgSelector id={link.icon} />              </Button>            </li>          ))}        </ul>      </div>    </nav>  );};export default Navigation;